apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: backend
spec:
  type: NodePort  # ALB Ingress Controller는 NodePort 필요!
  selector:
    app: backend
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP